"""Exogenous context features (Peruvian calendar + weather) for forecasting.

This package is the HU-08-07 groundwork: everything here is optional and
opt-in (`ForecastRequest.use_context`). No default request path touches this
package at all — see `app/forecasting/service.py`.
"""
